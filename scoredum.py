from score import parse
def score(inp, out):
    ns = parse(inp)
    itr = (line for line in out.split('\n'))
    # TODO: implement

    if __name__ == '__main__' and args.s:
        show(out)

    return 10
